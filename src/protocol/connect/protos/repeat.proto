syntax = "proto3";

package pleezer.playback.repeat.proto;

message Repeat {
  Mode repeat = 1;
}

enum Mode {
  REPEAT_MODE_NONE = 0;
  REPEAT_MODE_ALL = 1;
  REPEAT_MODE_ONE = 2;
  REPEAT_MODE_UNRECOGNIZED = -1;
}